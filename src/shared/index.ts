/**
 * SHARED MODULE
 * =============
 * Central export for all shared utilities, components, and hooks.
 *
 * Usage:
 * ```ts
 * import { Button, Card, ProductSwitcher } from "@/shared";
 * ```
 */

// UI Components
export * from "./components/ui";

// Layout Components
export * from "./components/layout";

// Hooks (to be added)
// export * from "./hooks";

// Lib utilities (to be added)
// export * from "./lib";

// Types (to be added)
// export * from "./types";
